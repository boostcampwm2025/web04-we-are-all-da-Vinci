name: PR Slack Notify
on:
  pull_request:
    types: [opened, reopened, synchronize] # PR 생성, 재오픈, 커밋 푸시 시
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send PR to Slack
        uses: slackapi/slack-github-action@v1.24.0
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        with:
          payload: |
            {
              "text": "🔔 *새 PR 생성됨* <#${{ github.event.pull_request.number }}|${{ github.event.pull_request.title }}> by <${{ github.actor }}|${{ github.actor }}> :boostcamp:",
              "username": "PR Bot",
              "icon_emoji": ":github:"
            }
